{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel watch index.html --dist-dir ../resources/webapp-test --public-url /foo",
    "build": "parcel build index.html --dist-dir ../resources/webapp-test --public-url /foo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.3.2"
  },
  "dependencies": {
    "@parcel/transformer-react-refresh-wrap": "^2.3.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
